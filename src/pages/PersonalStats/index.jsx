import { Card, Empty, Flex } from 'antd';

const PersonalStats = () => (
  <Flex style={{ minHeight: '100%', width: '100%' }} align="center" justify="center">
      <Empty description="个人统计建设中" />
  </Flex>
);

export default PersonalStats;
